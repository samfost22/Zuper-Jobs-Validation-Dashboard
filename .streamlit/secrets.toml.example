# Zuper API Configuration
# Copy this file to secrets.toml and fill in your credentials
# NEVER commit secrets.toml to git!

[zuper]
api_key = "your-zuper-api-key-here"
base_url = "https://us-east-1.zuperpro.com"  # or your Zuper region URL

# Slack Notifications (optional)
# Get a webhook URL from: https://api.slack.com/messaging/webhooks
[slack]
webhook_url = ""  # Leave empty to disable notifications

# For Streamlit Cloud deployment:
# 1. Go to your app settings on Streamlit Cloud
# 2. Click "Secrets" in the sidebar
# 3. Paste the contents of secrets.toml (without this comment block)
